import '../models/pq_models.dart';

/// Ported from process-quest/subjects/networks.js — all chapters, real
/// content, narrative bodies trimmed to plain text, callouts preserved as
/// hints, puzzles ported 1:1 with the same answers/explanations.
final networksChapters = <Chapter>[
  const Chapter(
    id: 1,
    title: "Welcome to Network Nation",
    avatar: "🐢",
    role: "Narrator — rolling into a city of wires",
    bodyIntro: "🎉 I just rolled my wheels into Network Nation! Everywhere I look, computers are talking to each other — 📱 phones, 💻 laptops, 🎮 game consoles, even 🧊 smart fridges. That's what a network is: a bunch of devices connected together so they can send messages back and forth.\n\n✉️ Think of it like kids passing notes in a classroom. One kid writes a note, passes it along, and it reaches the right friend. Now imagine a classroom so big it covers the whole planet 🌍 — that giant classroom is the Internet, a network made of millions of smaller networks all connected together.\n\n🔌 A network = devices connected so they can share messages\n\n🌐 The Internet = a network of networks, spanning the globe\n\n🏠 Your home Wi-Fi is a tiny network that plugs into that giant one\n\n✅ Let's check what you just learned.",
    calloutHints: [
      "🕰️ Fun fact: the first message ever sent between two computers, in 1969, was supposed to say \"LOGIN\" — but the system crashed after just \"LO\"! Even networks have bad days.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Which best describes the Internet?",
      options: [
        "One single giant computer that stores every website",
        "A network of networks — millions of smaller networks all connected together",
        "A type of software you install to make your computer faster",
        "A cable company that only exists in one country",
      ],
      answerIndex: 1,
      explainOk: "Yes! The Internet is huge because it's really lots and lots of smaller networks, all linked up so any device can talk to any other.",
      explainBad: "Not quite. The Internet isn't one computer or one company — it's countless smaller networks (homes, schools, offices) all wired together into one giant web.",
    ),
  ),
  const Chapter(
    id: 2,
    title: "Postcards Called Packets",
    avatar: "📬",
    role: "Narrator — chopping up a message",
    bodyIntro: "📝 Say I want to send you a big long letter — 5 whole pages! Instead of mailing it as one huge envelope, computers chop it into 5 tiny postcards called packets 📮📮📮📮📮, each one stamped \"page 1 of 5,\" \"page 2 of 5,\" and so on. Each packet travels on its own — maybe even by different roads! — and they get put back together in the right order once they all arrive.\n\n📍 But how does a packet know where to go? Every device on a network has its own address, called an IP address — just like your 🏠 house has a street address so mail finds it. An IP address looks like a set of numbers, like 192.168.1.7.\n\n👀 Watch the little package above zoom from \"You\" to the \"Server\" — that's exactly what a packet does, racing across wires and Wi-Fi signals to deliver your message!\n\n🧩 Match the packet vocabulary.",
    calloutHints: [
      "🔁 If one packet gets lost along the way (say, page 3 of 5 never shows up), computers are clever — they simply ask for that ONE postcard to be resent, instead of resending the whole 5-page letter.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a term on the left, then click its matching description on the right.",
      pairs: [
        MatchPair("packet", "📮 Packet", "One numbered piece of a big message, like '3 of 5'"),
        MatchPair("ip", "📍 IP address", "Every device's own address, so packets know where to go"),
      ],
      explainOk: "Exactly right — messages travel as packets, and every device has an IP address so packets always find their way home.",
    ),
  ),
  const Chapter(
    id: 3,
    title: "Ordering at the Server Café",
    avatar: "🍽️",
    role: "Narrator — visiting the request-and-response counter",
    bodyIntro: "🍔 Ever ordered food at a counter? You (the customer) ask for something, and the 🧑‍🍳 cook makes it and hands it back. Loading a webpage works exactly the same way!\n\n🙋 Your web browser is the client — like the hungry customer\n\n👨‍🍳 A powerful computer far away is the server — like the cook\n\n📤 Your browser sends a request (\"please give me this webpage!\")\n\n📦 The server sends back a response (the actual webpage, pictures and all)\n\n⚡ This back-and-forth happens every single time you open an app, watch a video, or play an online game. Millions of these little conversations are happening on the Internet right now, this very second!\n\n🔢 Put the steps of \"loading a webpage\" in the right order.",
    calloutHints: [
      "👥 One server can talk to thousands of clients at once — just like one cook can take orders from a whole line of hungry customers, one after another.",
    ],
    puzzleType: PuzzleType.order,
    order: OrderPuzzle(
      instructions: "Drag these steps into the order they actually happen when you open a webpage.",
      items: [
        OrderItem("type", "You type a website address into your browser"),
        OrderItem("request", "Your browser (the client) sends a request across the network"),
        OrderItem("travel", "The request travels as packets to the server"),
        OrderItem("server", "The server finds the webpage and sends a response"),
        OrderItem("show", "Your browser receives the response and shows you the page"),
      ],
      explainOk: "Perfect! That's the classic client-server dance: ask, travel, answer, travel back, show.",
      explainBad: "Almost — remember, you type first, then your browser sends the request, it travels to the server, the server answers, and finally your browser shows you the page.",
    ),
  ),
  const Chapter(
    id: 4,
    title: "Secret Handshakes Called Protocols",
    avatar: "🤝",
    role: "Narrator — learning the rules of the road",
    bodyIntro: "🤝 For two computers to understand each other, they need to agree on the rules of the conversation — like a secret handshake both sides know. These sets of rules are called protocols.\n\n🌐 HTTP — the protocol browsers and servers use to trade webpages (the \"http://\" or \"https://\" you see before website names!)\n\n✅ TCP — a careful protocol that double-checks every packet arrived, and resends anything that got lost. Great for things like loading a webpage, where you can't afford to miss a piece.\n\n⚡ UDP — a speedy protocol that doesn't bother double-checking. It just fires packets and hopes they arrive. Great for things like live video calls, where being fast matters more than being perfect.\n\n🎮 One last quiz before we roll on to the next level.",
    calloutHints: [
      "📬 Think of TCP like sending mail with tracking and a signature required — slower, but nothing gets lost. UDP is like tossing postcards into the wind — fast, but a few might not make it!",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click one card on the left and its matching card on the right to pair up each protocol with what it actually does.",
      pairs: [
        MatchPair("http", "🌐 HTTP", "Trades webpages between browser and server"),
        MatchPair("tcp", "✅ TCP", "Double-checks every packet arrived, resends anything lost"),
        MatchPair("udp", "⚡ UDP", "Fires packets fast without double-checking — great for live video"),
      ],
      explainOk: "Perfect matches! HTTP moves webpages, TCP is the careful one, and UDP is the speedy one. You've cracked every handshake in Network Nation. 🎉",
    ),
  ),
  const Chapter(
    id: 5,
    title: "Why Networks Have Layers",
    avatar: "🍰",
    role: "Narrator — slicing a layer cake",
    bodyIntro: "🍰 Level 2! Sending a message across a network isn't ONE big job — it's several smaller jobs stacked on top of each other, like layers in a cake. Each layer only worries about its own job, and hands things off to the layer below or above it.\n\nWhy bother splitting it up? Because if you ever swap out Wi-Fi for a wired cable, only the bottom layer needs to change — the layers above (like your web browser) don't care at all how the bits physically travel.\n\nThis layered idea is formalized as the OSI model — 7 layers we'll meet next.",
    calloutHints: [
      "🧱 Engineers call this separation of concerns: each layer does one job well, so the whole system is easier to build, fix, and upgrade.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "What's the main benefit of splitting networking into layers?",
      options: [
        "It makes the Internet run faster automatically",
        "Each layer can be built, understood, and swapped independently of the others",
        "It uses less electricity",
        "It means only one company can build networking hardware",
      ],
      answerIndex: 1,
      explainOk: "Right! Layering means you can change Wi-Fi to Ethernet without rewriting your browser — each layer is independent.",
      explainBad: "Think about maintenance: layering's superpower is that each layer can change on its own without breaking the others.",
    ),
  ),
  const Chapter(
    id: 6,
    title: "Meet the 7 OSI Layers",
    avatar: "📚",
    role: "Narrator — climbing the layer cake",
    bodyIntro: "The full OSI model has 7 layers, bottom to top:\n\nPhysical — actual cables, radio waves, electrical signals\n\nData Link — getting data across one local hop (MAC addresses)\n\nNetwork — getting data across many networks (IP addresses)\n\nTransport — reliable delivery, ordering (TCP/UDP)\n\nSession — keeping a conversation open\n\nPresentation — formatting/encryption of the data\n\nApplication — the actual app talking to you (HTTP, email)\n\nPut the layers in order from the bottom (closest to the wire) to the top (closest to you).",
    calloutHints: [
      "🧠 Memory trick: \"Please Do Not Throw Sausage Pizza Away\" — Physical, Data Link, Network, Transport, Session, Presentation, Application.",
    ],
    puzzleType: PuzzleType.order,
    order: OrderPuzzle(
      instructions: "Drag these layers from BOTTOM (physical wire) to TOP (the app you use).",
      items: [
        OrderItem("l1", "Physical — cables & signals"),
        OrderItem("l2", "Data Link — one local hop"),
        OrderItem("l3", "Network — across many networks (IP)"),
        OrderItem("l4", "Transport — reliable delivery (TCP/UDP)"),
        OrderItem("l5", "Session — keeps the conversation open"),
        OrderItem("l6", "Presentation — formatting/encryption"),
        OrderItem("l7", "Application — the app you actually use"),
      ],
      explainOk: "That's the whole cake, bottom to top! Every message you send climbs all 7 layers on the way out and back down on the way in.",
      explainBad: "Remember the mnemonic: Please (Physical) Do (Data Link) Not (Network) Throw (Transport) Sausage (Session) Pizza (Presentation) Away (Application).",
    ),
  ),
  const Chapter(
    id: 7,
    title: "The Simpler 4-Layer TCP/IP Model",
    avatar: "🧩",
    role: "Narrator — squishing 7 layers into 4",
    bodyIntro: "In practice, the real Internet runs on a simpler 4-layer model, the TCP/IP model, which squishes some OSI layers together:\n\nLink — combines Physical + Data Link\n\nInternet — same job as OSI's Network layer (IP addressing)\n\nTransport — same as OSI's Transport (TCP/UDP)\n\nApplication — combines Session + Presentation + Application\n\nMatch each TCP/IP layer to the OSI layers it stands in for.",
    calloutHints: [
      "📏 Both models describe the SAME real network — OSI is the detailed textbook version, TCP/IP is the practical version engineers actually talk about day to day.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a TCP/IP layer on the left, then click the OSI layers it covers on the right.",
      pairs: [
        MatchPair("link", "Link", "Physical + Data Link"),
        MatchPair("internet", "Internet", "Network"),
        MatchPair("transport", "Transport", "Transport"),
        MatchPair("app", "Application", "Session + Presentation + Application"),
      ],
      explainOk: "Exactly — TCP/IP's 4 layers are just OSI's 7 layers grouped more practically.",
    ),
  ),
  const Chapter(
    id: 8,
    title: "Encapsulation — Wrapping Envelopes",
    avatar: "✉️",
    role: "Narrator — stuffing envelopes inside envelopes",
    bodyIntro: "As your message travels DOWN the layers to be sent, each layer wraps it in its own little \"envelope\" of extra info called a header. This is encapsulation:\n\nApplication layer: your actual data (e.g. an HTTP request)\n\nTransport layer: wraps it with a TCP header (ports, sequence numbers)\n\nInternet layer: wraps THAT with an IP header (source/destination IP)\n\nLink layer: wraps THAT with a frame header (MAC addresses)\n\nWhen it arrives, the receiving computer unwraps each envelope in reverse — like peeling an onion — until it gets your original data back.",
    calloutHints: [
      "📦 Each layer only reads its OWN envelope and hands the rest inward — the Link layer never peeks at your HTTP data, it just delivers the whole wrapped package.",
    ],
    puzzleType: PuzzleType.order,
    order: OrderPuzzle(
      instructions: "Drag these steps into the order data gets wrapped on its way OUT (application → wire).",
      items: [
        OrderItem("data", "Your app creates the raw data (e.g. an HTTP request)"),
        OrderItem("tcp", "Transport layer adds a TCP header (ports, sequencing)"),
        OrderItem("ip", "Internet layer adds an IP header (source/destination address)"),
        OrderItem("frame", "Link layer adds a frame header (MAC addresses) and sends it on the wire"),
      ],
      explainOk: "That's encapsulation — each layer adds its own header on the way out, like envelopes inside envelopes.",
      explainBad: "Think top-down: your app's data is wrapped first by Transport (TCP), then Internet (IP), then Link (frame) right before it hits the wire.",
    ),
  ),
  const Chapter(
    id: 9,
    title: "Every Device Needs an Address",
    avatar: "🏠",
    role: "Narrator — reading house numbers",
    bodyIntro: "An IPv4 address looks like 192.168.1.7 — four numbers (each 0-255) separated by dots, called octets because each one is 8 bits.\n\nJust like a street address has (house number, street, city, country), an IP address has a structure too: part of it identifies WHICH network you're on, and part identifies WHICH device on that network — like \"Apartment building #5, Unit 12.\"\n\nWhich of these is a VALID IPv4 address?",
    calloutHints: [
      "🔢 8 bits per octet means each number can only go from 0 to 255 (2⁸ = 256 possible values, starting at 0) — that's why you never see 192.168.1.900.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Which of these could be a real IPv4 address?",
      options: [
        "10.0.0.256",
        "300.1.1.1",
        "192.168.1.7",
        "1.2.3",
      ],
      answerIndex: 2,
      explainOk: "Right! 192.168.1.7 — four octets, each between 0 and 255.",
      explainBad: "Each octet must be 0-255 and there must be exactly 4 of them. 256 and 300 are too big, and '1.2.3' is missing an octet.",
    ),
  ),
  const Chapter(
    id: 10,
    title: "Private vs Public Addresses",
    avatar: "🏘️",
    role: "Narrator — comparing neighborhoods",
    bodyIntro: "Not all IP addresses are visible to the whole Internet. Private addresses (like 192.168.x.x or 10.x.x.x) only work inside your home or office network — like an internal apartment number that only makes sense inside the building.\n\nPublic addresses are unique across the ENTIRE Internet — like your building's real street address that mail trucks anywhere in the world can find.\n\nYour home router usually does NAT (Network Address Translation): it gives every device in your house a private address, but shares ONE public address with the outside world.",
    calloutHints: [
      "🚪 That's why a website only ever sees your router's ONE public IP, even though 10 devices in your house are all browsing it at once.",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "Drag each address type into the right bucket.",
      bucketALabel: "🏠 Private (inside your network only)",
      bucketBLabel: "🌍 Public (unique across the whole Internet)",
      items: [
        Sort2Item("a", "192.168.1.7 — your laptop at home", true),
        Sort2Item("b", "10.0.0.5 — a printer on the office LAN", true),
        Sort2Item("c", "Your router's one address that websites actually see", false),
        Sort2Item("d", "A web server's address that anyone on Earth can reach", false),
      ],
      explainOk: "Exactly — private addresses only make sense inside your own network; public addresses are globally unique.",
      explainBad: "Remember: anything starting with 192.168 or 10. is private (local-only); the address the outside world actually sees is public.",
    ),
  ),
  const Chapter(
    id: 11,
    title: "Subnet Masks — Splitting the Neighborhood",
    avatar: "✂️",
    role: "Narrator — drawing lines on a map",
    bodyIntro: "A subnet mask (like 255.255.255.0) tells a device where the \"network part\" of an IP address ends and the \"device part\" begins.\n\nWith mask 255.255.255.0, the first 3 octets (192.168.1) are the NETWORK, and the last octet (.7) is the specific DEVICE. Every device with the same first 3 octets is a neighbor on the same local network.\n\nSame street or different street? Sort these device pairs.",
    calloutHints: [
      "🏘️ Think of it like a street name vs a house number: everyone on \"192.168.1.*\" street can talk directly; to reach a different street, you need a router.",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "With mask 255.255.255.0, sort each pair of IPs into the right bucket.",
      bucketALabel: "✅ Same network (same first 3 octets)",
      bucketBLabel: "🚧 Different network (need a router)",
      items: [
        Sort2Item("p1", "192.168.1.5 and 192.168.1.9", true),
        Sort2Item("p2", "192.168.1.5 and 192.168.2.9", false),
        Sort2Item("p3", "10.0.0.1 and 10.0.0.2", true),
        Sort2Item("p4", "10.0.0.1 and 10.0.1.1", false),
      ],
      explainOk: "Right — with a /24 mask, only the LAST octet can differ and still be on the same network.",
      explainBad: "With mask 255.255.255.0, compare the first THREE octets only — if they match, it's the same network.",
    ),
  ),
  const Chapter(
    id: 12,
    title: "CIDR — The Shorthand for Masks",
    avatar: "🖊️",
    role: "Narrator — writing addresses the fast way",
    bodyIntro: "Writing 255.255.255.0 every time is a mouthful, so engineers use CIDR notation: 192.168.1.0/24. The /24 means \"the first 24 bits (3 octets) are the network part\" — exactly the same as that mask.\n\n/24 = 255.255.255.0 → 256 addresses, ~254 usable devices\n\n/16 = 255.255.0.0 → a much BIGGER network, ~65,000 devices\n\nSmaller slash number = bigger network (fewer bits reserved for \"network\")\n\nWhich network can fit MORE devices?",
    calloutHints: [
      "🔢 Counterintuitive but true: /16 is a BIGGER network than /24, because it leaves more bits free for individual devices.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Which of these CIDR networks can hold the MOST devices?",
      options: [
        "192.168.1.0/28",
        "192.168.1.0/24",
        "10.0.0.0/16",
        "192.168.1.0/30",
      ],
      answerIndex: 2,
      explainOk: "Right! /16 leaves the most bits for devices, so it's the biggest of these networks — about 65,000 addresses.",
      explainBad: "Remember: the SMALLER the number after the slash, the BIGGER the network. /16 beats /24, which beats /28 and /30.",
    ),
  ),
  const Chapter(
    id: 13,
    title: "Hopping Between Networks",
    avatar: "🦘",
    role: "Narrator — meeting a router",
    bodyIntro: "A router is a device that connects DIFFERENT networks together and decides which direction to forward a packet — like a highway interchange choosing which exit gets you closer to your destination.\n\nA packet crossing the Internet usually passes through MANY routers, one hop at a time, each one forwarding it a little closer to its destination — similar to a relay race handing off a baton.\n\nMatch the routing vocabulary.",
    calloutHints: [
      "🛣️ Your home router does double duty: it routes traffic between your home network and the Internet, AND does NAT to share one public address.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a term on the left, then its matching description on the right.",
      pairs: [
        MatchPair("router", "🦘 Router", "Connects different networks and forwards packets between them"),
        MatchPair("hop", "🎯 Hop", "One step of a packet's journey from one router to the next"),
      ],
      explainOk: "Right — routers connect networks, and each router-to-router step is one hop.",
    ),
  ),
  const Chapter(
    id: 14,
    title: "The Router's Map: Routing Tables",
    avatar: "🗺️",
    role: "Narrator — reading a router's notebook",
    bodyIntro: "Every router keeps a routing table — a little notebook of \"if the destination looks like THIS, send it out THAT direction.\"\n\nDestination — Next Hop — Interface\n\n192.168.1.0/24 — directly connected — eth0\n\n10.0.0.0/8 — 10.0.0.1 — eth1\n\n0.0.0.0/0 (everything else) — 203.0.113.1 — wan0\n\nThat last row, 0.0.0.0/0, is the default route — \"if nothing else matches, send it toward the Internet.\"",
    calloutHints: [
      "🧭 Routers pick the MOST SPECIFIC matching row, not just the first one — a /24 match beats the /0 default route every time.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Using the table above, where does a packet headed to 192.168.1.50 get sent?",
      options: [
        "Out wan0 toward 203.0.113.1",
        "Out eth1 toward 10.0.0.1",
        "Out eth0, directly connected",
        "The router drops it",
      ],
      answerIndex: 2,
      explainOk: "Right! 192.168.1.50 matches the 192.168.1.0/24 row exactly, so it goes out eth0 directly — no need for the default route.",
      explainBad: "Look for the MOST SPECIFIC matching row first — 192.168.1.50 falls inside 192.168.1.0/24, which is a direct match.",
    ),
  ),
  const Chapter(
    id: 15,
    title: "Static vs Dynamic Routing",
    avatar: "🔀",
    role: "Narrator — comparing two routers' styles",
    bodyIntro: "Static routing: a human manually types in every route. Simple and predictable, but doesn't adapt if a link goes down — great for small, stable networks.\n\nDynamic routing: routers talk to EACH OTHER using routing protocols (like OSPF or BGP) to automatically discover the best paths and reroute around failures — essential for the massive, ever-changing Internet.\n\nSort these scenarios into static or dynamic.",
    calloutHints: [
      "🏢 A small office might use static routes for its 2 routers. The Internet backbone, connecting thousands of networks, MUST use dynamic routing.",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "Drag each scenario into the routing style that fits best.",
      bucketALabel: "✍️ Static routing",
      bucketBLabel: "🔄 Dynamic routing",
      items: [
        Sort2Item("s1", "A tiny home network with just one router to the ISP", true),
        Sort2Item("s2", "The global Internet backbone, constantly rerouting around outages", false),
        Sort2Item("s3", "A network engineer manually types every route by hand", true),
        Sort2Item("s4", "Routers automatically discover new best paths using OSPF/BGP", false),
      ],
      explainOk: "Exactly — static is simple/manual for small stable networks; dynamic adapts automatically for large, ever-changing ones.",
      explainBad: "Static = manually configured, unchanging. Dynamic = routers talk to each other and adapt automatically.",
    ),
  ),
  const Chapter(
    id: 16,
    title: "Counting Hops: Finding the Best Path",
    avatar: "📏",
    role: "Narrator — comparing two routes",
    bodyIntro: "When there are multiple paths to a destination, routers pick using a metric — often the simplest one is hop count: how many routers you'd have to pass through.\n\nBut fewer hops isn't always faster! A 2-hop path over a congested, slow satellite link could be much slower than a 5-hop path over fast fiber cables. Real protocols often weigh in bandwidth and delay too, not just hop count.\n\nWhich path is generally preferred by simple hop-count routing?",
    calloutHints: [
      "🐢 Just like Process the turtle knows: the \"shortest\" path on a map isn't always the FASTEST path in practice — traffic and road quality matter too.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "A hop-count-based router chooses between a 2-hop path and a 5-hop path to the same destination. Which does it prefer?",
      options: [
        "The 5-hop path, because more routers means more reliability",
        "The 2-hop path, because hop count is lower",
        "It always picks randomly",
        "It refuses to send the packet",
      ],
      answerIndex: 1,
      explainOk: "Right! Pure hop-count routing always prefers the path with fewer hops, even if that's not always truly the fastest in reality.",
      explainBad: "Hop-count routing is simple: fewer hops wins, even though real-world speed can depend on more than just hop count.",
    ),
  ),
  const Chapter(
    id: 17,
    title: "TCP vs UDP, Revisited",
    avatar: "⚖️",
    role: "Narrator — weighing reliability against speed",
    bodyIntro: "We met TCP and UDP back in Level 1 — now let's go deeper. Both live at the Transport layer and both use port numbers to know WHICH app on a device a packet belongs to (like apartment unit numbers within a building's one street address).\n\nTCP — connection-based, guarantees delivery and order, has more overhead\n\nUDP — connectionless, no guarantees, minimal overhead, very fast\n\nSort these use cases by which protocol fits best.",
    calloutHints: [
      "🎮 Video games often use UDP for fast-moving position updates (a missed update just gets overwritten by the next one) but TCP for chat messages (you can't afford to lose a word).",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "Drag each use case into the protocol that suits it best.",
      bucketALabel: "✅ TCP (must arrive, in order)",
      bucketBLabel: "⚡ UDP (fast, ok to drop some)",
      items: [
        Sort2Item("u1", "Downloading a file — every byte must be correct", true),
        Sort2Item("u2", "Live video call — a dropped frame is fine, just keep going", false),
        Sort2Item("u3", "Loading a webpage's HTML", true),
        Sort2Item("u4", "Fast-paced online game position updates", false),
      ],
      explainOk: "Exactly — TCP for anything that must be complete and correct, UDP for anything where speed beats perfection.",
      explainBad: "Ask: can I tolerate losing a little data for more speed? If no → TCP. If yes → UDP.",
    ),
  ),
  const Chapter(
    id: 18,
    title: "The Three-Way Handshake",
    avatar: "🤝",
    role: "Narrator — watching two computers say hello",
    bodyIntro: "Before TCP sends any real data, the client and server perform a three-way handshake to agree they're both ready:\n\nSYN — client: \"Hey, can we talk? Here's my starting sequence number.\"\n\nSYN-ACK — server: \"Sure! Here's mine too, and I acknowledge yours.\"\n\nACK — client: \"Got it, let's go!\"\n\nPut the handshake steps in order.",
    calloutHints: [
      "🎬 Only AFTER all three steps finish does real data start flowing — this is why a brand-new TCP connection has a tiny bit of extra delay before anything happens.",
    ],
    puzzleType: PuzzleType.order,
    order: OrderPuzzle(
      instructions: "Drag these into the correct handshake order.",
      items: [
        OrderItem("syn", "Client sends SYN (\"can we talk?\")"),
        OrderItem("synack", "Server replies SYN-ACK (\"yes, and here's mine\")"),
        OrderItem("ack", "Client sends ACK (\"got it, let's go\")"),
        OrderItem("data", "Real data starts flowing both ways"),
      ],
      explainOk: "That's the three-way handshake — SYN, SYN-ACK, ACK — then and only then does data flow.",
      explainBad: "Remember the order: the client always speaks first (SYN), the server replies (SYN-ACK), then the client confirms (ACK).",
    ),
  ),
  const Chapter(
    id: 19,
    title: "Sequence Numbers & Acknowledgments",
    avatar: "🔢",
    role: "Narrator — numbering postcards",
    bodyIntro: "TCP labels every byte it sends with a sequence number, so the receiver can put pieces back in the right order even if they arrive scrambled — just like numbered pages of our 5-page letter from Level 1.\n\nThe receiver sends back an ACK (acknowledgment) saying \"I got up to byte X.\" If the sender doesn't see an ACK in time, it assumes the data was lost and retransmits it.\n\nWhich happens if the sender never receives an ACK in time?",
    calloutHints: [
      "⏱️ This \"wait for ACK, resend if missing\" loop is exactly how TCP guarantees delivery — no central authority, just senders and receivers politely confirming.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "A TCP sender sends data but never gets an ACK back in time. What does it do?",
      options: [
        "Gives up immediately and closes the connection",
        "Retransmits the unacknowledged data",
        "Ignores it — UDP-style behavior",
        "Sends a SYN packet again",
      ],
      answerIndex: 1,
      explainOk: "Right! TCP assumes the data was lost and retransmits it — that's the core of its reliability guarantee.",
      explainBad: "TCP's whole reliability trick is retransmission: if no ACK arrives in time, it resends the data, it doesn't just give up.",
    ),
  ),
  const Chapter(
    id: 20,
    title: "Congestion Control — Don't Flood the Road",
    avatar: "🚦",
    role: "Narrator — watching traffic build up",
    bodyIntro: "If every sender blasted data as fast as possible, networks would jam up like rush hour traffic. Congestion control is TCP's way of easing off the gas when the network gets busy.\n\nTCP starts sending SLOWLY (called slow start), then speeds up as ACKs keep confirming things are fine. If packets start getting lost, TCP assumes congestion and slows back down — like a driver easing off the accelerator when traffic thickens.\n\nWhat's the likely cause when TCP suddenly slows its sending rate?",
    calloutHints: [
      "🚗 This self-adjusting behavior is why thousands of connections can share the same busy Internet link without completely gridlocking it.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "A TCP connection that was sending fast suddenly cuts its rate way down. What most likely happened?",
      options: [
        "The user paused the download on purpose",
        "It detected packet loss and assumed network congestion",
        "The server ran out of disk space",
        "The client's IP address changed",
      ],
      answerIndex: 1,
      explainOk: "Exactly — packet loss is TCP's main signal that the network is congested, so it backs off to avoid making things worse.",
      explainBad: "TCP congestion control reacts to LOST packets — that's its main clue that the network path is overloaded.",
    ),
  ),
  const Chapter(
    id: 21,
    title: "Closing the Connection",
    avatar: "👋",
    role: "Narrator — saying goodbye politely",
    bodyIntro: "Just like TCP opens politely with a handshake, it closes politely too — usually with a FIN (finish) and ACK exchange from both sides, so neither computer is left wondering if the other one crashed or just finished talking.\n\nMatch the connection lifecycle stage to its TCP flag.",
    calloutHints: [
      "📴 UDP has NO such goodbye — since it never opened a connection in the first place, there's nothing formal to close. It just stops sending.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a stage on the left, then its TCP flag on the right.",
      pairs: [
        MatchPair("open", "Opening a new connection", "SYN"),
        MatchPair("confirm", "Confirming data or the open/close request", "ACK"),
        MatchPair("close", "Politely ending the connection", "FIN"),
      ],
      explainOk: "Right — SYN opens, ACK confirms, FIN closes. A full TCP connection lifecycle in three flags.",
    ),
  ),
  const Chapter(
    id: 22,
    title: "DNS — The Internet's Phonebook",
    avatar: "📖",
    role: "Narrator — looking up a name",
    bodyIntro: "You type example.com, not 93.184.216.34 — humans are much better at remembering names than numbers! DNS (Domain Name System) is the service that translates domain names into IP addresses.\n\nWhat does DNS actually do?",
    calloutHints: [
      "📞 DNS is exactly like an old phonebook: you look up a NAME and get back a NUMBER (the IP address) to actually dial/connect to.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "What is DNS's main job?",
      options: [
        "Encrypting your Internet traffic",
        "Translating human-readable domain names into IP addresses",
        "Speeding up your Wi-Fi signal",
        "Storing your saved passwords",
      ],
      answerIndex: 1,
      explainOk: "Right! DNS is the Internet's phonebook, turning names like example.com into IP addresses computers use to connect.",
      explainBad: "DNS's ONE job is name→address translation — not encryption, Wi-Fi speed, or password storage.",
    ),
  ),
  const Chapter(
    id: 23,
    title: "The DNS Resolution Chain",
    avatar: "🔗",
    role: "Narrator — following the phonebook trail",
    bodyIntro: "Looking up a domain isn't just ONE lookup — it's a CHAIN of them:\n\nYour computer asks a resolver (often run by your ISP)\n\nThe resolver asks a root server (\"who handles .com?\")\n\nThe root points to a TLD server for .com\n\nThe TLD server points to the authoritative server for example.com\n\nThat authoritative server FINALLY returns the real IP address\n\nTrace the chain in order.",
    calloutHints: [
      "⚡ Once looked up, the answer is cached for a while so this whole chain doesn't need to repeat every single time you visit the same site.",
    ],
    puzzleType: PuzzleType.order,
    order: OrderPuzzle(
      instructions: "Drag these into the order a fresh DNS lookup actually happens.",
      items: [
        OrderItem("you", "Your computer asks its resolver for example.com"),
        OrderItem("root", "Resolver asks a root server \"who handles .com?\""),
        OrderItem("tld", "Root points to the .com TLD server"),
        OrderItem("auth", "TLD server points to example.com's authoritative server"),
        OrderItem("answer", "Authoritative server returns the real IP address"),
      ],
      explainOk: "That's the full DNS resolution chain — root, then TLD, then authoritative, then finally the real answer.",
      explainBad: "It always narrows down: root (top) → TLD (.com) → authoritative (example.com) → the actual IP.",
    ),
  ),
  const Chapter(
    id: 24,
    title: "HTTP Request/Response Anatomy",
    avatar: "📨",
    role: "Narrator — reading the fine print of a request",
    bodyIntro: "An HTTP request has a few key parts: a method (like GET or POST), a path (like /home), headers (extra info like what browser you're using), and sometimes a body (data you're sending, like a submitted form).\n\nThe response comes back with a status code — 200 means \"all good,\" 404 means \"not found,\" 500 means \"the server broke.\"\n\nMatch each status code to its meaning.",
    calloutHints: [
      "🚦 Status codes are grouped: 2xx = success, 3xx = redirect, 4xx = your request had a problem, 5xx = the server had a problem.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a code on the left, then its meaning on the right.",
      pairs: [
        MatchPair("200", "200", "OK — everything worked"),
        MatchPair("404", "404", "Not Found — that page doesn't exist"),
        MatchPair("500", "500", "Internal Server Error — the server broke"),
      ],
      explainOk: "Right — 200 success, 404 missing page, 500 server-side failure. You'll see these constantly as a web developer.",
    ),
  ),
  const Chapter(
    id: 25,
    title: "An Email's Journey (SMTP)",
    avatar: "📧",
    role: "Narrator — following a letter through the mail system",
    bodyIntro: "Sending email uses SMTP (Simple Mail Transfer Protocol). Your email app hands your message to YOUR mail server, which then finds the RECIPIENT's mail server (often by doing a DNS lookup for a special \"MX record\") and delivers it there. The recipient then downloads it from their server using a different protocol.\n\nOrder the steps of an email's journey.",
    calloutHints: [
      "✉️ SMTP is like the postal service between mail sorting offices — your local post office doesn't deliver directly to a stranger's mailbox across the world, it hands the letter off, office to office, until it reaches the right one.",
    ],
    puzzleType: PuzzleType.order,
    order: OrderPuzzle(
      instructions: "Drag these into the order an email actually travels.",
      items: [
        OrderItem("compose", "You compose and hit send in your email app"),
        OrderItem("yourserver", "Your mail server receives it via SMTP"),
        OrderItem("lookup", "Your mail server looks up the recipient's mail server (via DNS MX record)"),
        OrderItem("deliver", "Your mail server delivers it to the recipient's mail server"),
        OrderItem("download", "The recipient downloads it from their server into their inbox"),
      ],
      explainOk: "Exactly right — email hops server to server via SMTP before the recipient ever downloads it.",
      explainBad: "Your app never talks directly to the recipient — it always goes through mail servers first, found via a DNS lookup.",
    ),
  ),
  const Chapter(
    id: 26,
    title: "Why Networks Need Locks",
    avatar: "🔒",
    role: "Narrator — noticing an unlocked door",
    bodyIntro: "Every packet you send could, in theory, be read by devices along its path — like a postcard anyone handling the mail could peek at. That's a problem for passwords, credit cards, and private messages.\n\nNetwork security is about protecting data as it travels, and protecting devices from unwanted visitors trying to get in.",
    calloutHints: [
      "🕵️ \"https://\" instead of \"http://\" means the connection is ENCRYPTED — like sealing that postcard inside a locked envelope only the right recipient can open.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "What does the 's' in https actually mean?",
      options: [
        "It stands for 'server'",
        "The connection is encrypted/secured",
        "The site loads faster",
        "It's a newer version of HTML",
      ],
      answerIndex: 1,
      explainOk: "Right! The 's' means Secure — your data is encrypted in transit, so eavesdroppers can't read it.",
      explainBad: "The 's' in https specifically means Secure — encrypted — not speed, servers, or HTML version.",
    ),
  ),
  const Chapter(
    id: 27,
    title: "The TLS Handshake",
    avatar: "🔐",
    role: "Narrator — watching a secret get exchanged safely",
    bodyIntro: "TLS (Transport Layer Security) is what makes HTTP become HTTPS. Before any real data flows, client and server perform a TLS handshake:\n\nClient says hello and lists encryption methods it supports\n\nServer picks one, and sends back its certificate (proof of identity)\n\nBoth sides agree on a shared secret key, without ever sending that key in the open\n\nAll further data is encrypted using that shared secret\n\nOrder the TLS handshake steps.",
    calloutHints: [
      "🪪 The certificate is like a notarized ID card — it's signed by a trusted authority, so your browser can verify the server really is who it claims to be.",
    ],
    puzzleType: PuzzleType.order,
    order: OrderPuzzle(
      instructions: "Drag these into the order they actually happen.",
      items: [
        OrderItem("hello", "Client says hello, lists supported encryption methods"),
        OrderItem("cert", "Server replies with its certificate (proof of identity)"),
        OrderItem("key", "Both sides agree on a shared secret key"),
        OrderItem("encrypted", "All further data is encrypted with that shared key"),
      ],
      explainOk: "That's the TLS handshake — hello, certificate, shared key, then encrypted data flows.",
      explainBad: "The server must prove its identity (certificate) BEFORE any shared secret is agreed on — trust first, then encryption.",
    ),
  ),
  const Chapter(
    id: 28,
    title: "Firewalls — The Bouncer",
    avatar: "🚧",
    role: "Narrator — meeting the bouncer at the door",
    bodyIntro: "A firewall checks every packet trying to enter or leave a network against a set of rules, and blocks anything that doesn't belong — like a bouncer checking IDs at a club door.\n\nALLOW  from: any        to: port 443 (HTTPS)\n\nALLOW  from: any        to: port 80  (HTTP)\n\nDENY   from: any        to: port 23  (old insecure Telnet)\n\nDENY   from: unknown_IP to: port 22  (SSH admin access)\n\nUsing the rules above, what happens to unwanted Telnet traffic?",
    calloutHints: [
      "🛡️ Firewalls can filter by port, by IP address, or even by looking inside the packet's content — modern ones combine several techniques at once.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Based on the rule table above, what happens to traffic aimed at port 23 (Telnet)?",
      options: [
        "It's allowed through, same as HTTPS",
        "It's denied/blocked by the firewall",
        "It's automatically redirected to port 443",
        "The firewall ignores port numbers entirely",
      ],
      answerIndex: 1,
      explainOk: "Right! The rule explicitly says DENY for port 23 — Telnet traffic gets blocked at the firewall.",
      explainBad: "Look at the rule for port 23 specifically — it says DENY, meaning that traffic is blocked.",
    ),
  ),
  const Chapter(
    id: 29,
    title: "VPNs — The Secret Tunnel",
    avatar: "🚇",
    role: "Narrator — ducking into a hidden tunnel",
    bodyIntro: "A VPN (Virtual Private Network) creates an encrypted \"tunnel\" between your device and a VPN server, so anyone watching the network in between (like on public Wi-Fi) only sees scrambled, unreadable traffic — and websites see the VPN server's IP address, not yours.\n\nWhat's the MAIN thing a VPN protects against?",
    calloutHints: [
      "🕳️ Think of a VPN like a private, soundproofed tunnel dug underneath a public street — the street (public network) is still there, but nobody outside the tunnel can see or hear what's moving through it.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "What is a VPN's main protection?",
      options: [
        "It makes your Internet connection permanently faster",
        "It hides/encrypts your traffic from others on the same network path and masks your real IP",
        "It removes the need for passwords entirely",
        "It automatically blocks all viruses",
      ],
      answerIndex: 1,
      explainOk: "Right — a VPN's core job is encrypting your traffic and hiding your real IP from the sites/networks you connect through.",
      explainBad: "A VPN isn't a speed booster or an antivirus — its core job is privacy: encrypting your traffic and masking your IP.",
    ),
  ),
  const Chapter(
    id: 30,
    title: "Ethernet & Wired LANs",
    avatar: "🔌",
    role: "Narrator — plugging in a cable",
    bodyIntro: "Ethernet is the most common wired LAN (Local Area Network) technology — the standard for how devices physically connect via cables and exchange frames (Link-layer packets) with each other.\n\nEach Ethernet device has a MAC address burned into its network card — a unique hardware ID, unlike an IP address which can change.",
    calloutHints: [
      "🏷️ Think of a MAC address like a serial number stamped on a device at the factory — it doesn't change even if you move the device to a totally new network.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "What's the key difference between a MAC address and an IP address?",
      options: [
        "They're exactly the same thing",
        "MAC is a fixed hardware ID; IP can change depending on what network you join",
        "IP addresses never change, MAC addresses change constantly",
        "MAC addresses are only used for Wi-Fi, never Ethernet",
      ],
      answerIndex: 1,
      explainOk: "Right! MAC = fixed hardware ID from the factory. IP = assigned by whatever network you're currently connected to.",
      explainBad: "MAC addresses are burned in at the factory (fixed); IP addresses are assigned per-network and can change.",
    ),
  ),
  const Chapter(
    id: 31,
    title: "Switches vs Hubs — Collision Domains",
    avatar: "🔀",
    role: "Narrator — comparing two old office tools",
    bodyIntro: "An old-school hub repeats every signal to EVERY connected device — if two devices send at once, their signals collide and both have to resend. All devices on a hub share one collision domain.\n\nA switch is smarter: it learns which MAC address is on which port, and sends frames ONLY to the right port — dramatically reducing collisions, since each port is basically its own tiny collision domain.\n\nSort hub vs switch behavior.",
    calloutHints: [
      "📢 A hub is like shouting a message to an entire room hoping the right person hears it. A switch is like handing a sealed note directly to the right person.",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "Drag each behavior into the device that matches it.",
      bucketALabel: "📢 Hub",
      bucketBLabel: "🔀 Switch",
      items: [
        Sort2Item("h1", "Repeats every signal to every connected device", true),
        Sort2Item("h2", "Learns MAC addresses and forwards only to the right port", false),
        Sort2Item("h3", "All ports share one big collision domain", true),
        Sort2Item("h4", "Each port is effectively its own collision domain", false),
      ],
      explainOk: "Exactly — hubs broadcast blindly, switches learn and forward intelligently.",
      explainBad: "Hubs repeat to everyone (dumb); switches learn MAC-to-port mappings and forward smartly.",
    ),
  ),
  const Chapter(
    id: 32,
    title: "Wi-Fi Basics",
    avatar: "📶",
    role: "Narrator — watching invisible radio waves",
    bodyIntro: "Wi-Fi does the same basic job as Ethernet (Link-layer delivery) but over radio waves instead of cables. An access point (AP) is the device (often built into your home router) that devices connect to wirelessly.\n\nBecause radio is a SHARED medium — everyone's signals share the same \"air\" — Wi-Fi has to carefully coordinate who's \"talking\" to avoid constant collisions, which is part of why Wi-Fi is often a bit slower and less consistent than a wired connection.",
    calloutHints: [
      "📡 More devices on the same Wi-Fi network = more competition for that shared airtime, which is why a crowded coffee shop's Wi-Fi often feels sluggish.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Why does Wi-Fi often slow down when many devices join the same network?",
      options: [
        "Wi-Fi routers get physically tired",
        "All devices share the same radio airtime, so more devices means more competition for it",
        "Wi-Fi has a strict 5-device limit",
        "It's always the ISP's fault, never the local network",
      ],
      answerIndex: 1,
      explainOk: "Right! Wi-Fi is a shared medium — more devices competing for the same airtime means less bandwidth for each.",
      explainBad: "The key idea: Wi-Fi devices SHARE the same radio airtime, so congestion grows as more devices join.",
    ),
  ),
  const Chapter(
    id: 33,
    title: "Reading a MAC Address Table",
    avatar: "📋",
    role: "Narrator — peeking inside a switch's memory",
    bodyIntro: "A switch keeps a small table mapping MAC addresses to the port they were last seen on:\n\nMAC Address        Port\n\nAA:BB:CC:11:22:33   Port 1\n\nDD:EE:FF:44:55:66   Port 2\n\n11:22:33:AA:BB:CC   Port 3\n\nWhen a frame arrives addressed to DD:EE:FF:44:55:66, the switch checks this table and forwards it ONLY out Port 2 — not to every port like a hub would.",
    calloutHints: [
      "❓ If a destination MAC ISN'T in the table yet, the switch temporarily floods it to all ports (just once) until it learns the real answer from a reply.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Using the table above, a frame arrives addressed to 11:22:33:AA:BB:CC. Which port does the switch send it out?",
      options: [
        "Port 1",
        "Port 2",
        "Port 3",
        "All ports, like a hub",
      ],
      answerIndex: 2,
      explainOk: "Right! The table maps that MAC address directly to Port 3.",
      explainBad: "Look up 11:22:33:AA:BB:CC in the table — it's listed against Port 3.",
    ),
  ),
  const Chapter(
    id: 34,
    title: "Latency vs Bandwidth",
    avatar: "⏱️",
    role: "Narrator — comparing a highway's width and speed limit",
    bodyIntro: "Bandwidth is how MUCH data can flow per second — like how many lanes a highway has. Latency is how LONG one piece of data takes to make the trip — like the actual driving time for one single car.\n\nA connection can have huge bandwidth but still feel laggy if latency is high — imagine a 20-lane highway where every car still takes an hour to cross, because the road is just that long (like a satellite link to space and back).\n\nSort these complaints by which metric they're really about.",
    calloutHints: [
      "🎮 Online gaming cares MOSTLY about low latency (fast reaction time); streaming a movie cares MOSTLY about high bandwidth (enough data flowing per second).",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "Drag each complaint into the metric it's actually describing.",
      bucketALabel: "⏱️ Latency (delay per trip)",
      bucketBLabel: "📊 Bandwidth (data per second)",
      items: [
        Sort2Item("c1", "\"My video call has a noticeable lag before I hear a reply\"", true),
        Sort2Item("c2", "\"My 4K movie keeps buffering because too little data arrives per second\"", false),
        Sort2Item("c3", "\"My competitive game feels delayed even though downloads are fast\"", true),
        Sort2Item("c4", "\"Downloading a huge file is slow because my connection can't push enough data at once\"", false),
      ],
      explainOk: "Right — delay-per-trip complaints are latency; total-data-per-second complaints are bandwidth. They're independent problems!",
      explainBad: "Ask: is the complaint about DELAY (latency) or about total THROUGHPUT (bandwidth)? They can be good or bad independently of each other.",
    ),
  ),
  const Chapter(
    id: 35,
    title: "Ping & Traceroute",
    avatar: "🛰️",
    role: "Narrator — pinging the void and listening for an echo",
    bodyIntro: "ping sends a tiny packet to a destination and measures how long the reply takes to come back (round-trip time) — a quick way to check \"is this even reachable, and how laggy is it?\"\n\ntraceroute goes further: it shows EVERY router hop along the way to a destination, and the delay at each one — great for spotting exactly WHERE a slowdown is happening.\n\nMatch the tool to its purpose.",
    calloutHints: [
      "🔍 If ping shows high latency and traceroute shows the delay spikes at hop 4 out of 10, you've just found the likely trouble spot on the path.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a tool on the left, then its purpose on the right.",
      pairs: [
        MatchPair("ping", "ping", "Checks if a destination is reachable and measures round-trip time"),
        MatchPair("trace", "traceroute", "Shows every router hop along the path and the delay at each one"),
      ],
      explainOk: "Right — ping answers 'is it reachable and how slow', traceroute answers 'exactly where is it slow'.",
    ),
  ),
  const Chapter(
    id: 36,
    title: "Packet Loss — What Goes Wrong",
    avatar: "📉",
    role: "Narrator — counting missing postcards",
    bodyIntro: "Packet loss happens when packets never arrive — dropped by a congested router, a faulty cable, or radio interference on Wi-Fi. A LITTLE loss is often invisible (TCP just retransmits); a LOT of loss causes stutter, freezing, or dropped calls.\n\nWhich of these is a LIKELY real-world cause of packet loss?",
    calloutHints: [
      "📶 A common real-world cause: a microwave or cordless phone using the same radio frequency as your Wi-Fi, causing interference bursts and dropped packets right when you microwave your lunch!",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Which of these is a realistic cause of packet loss on a home Wi-Fi network?",
      options: [
        "The router's clock being one minute off",
        "Radio interference from a nearby microwave or cordless phone on the same frequency",
        "Using a website with too many colors",
        "The browser tab being minimized",
      ],
      answerIndex: 1,
      explainOk: "Right! Radio interference from other devices on the same frequency band is a very real cause of Wi-Fi packet loss.",
      explainBad: "Think physical/network layer causes — interference, congestion, faulty cables — not UI details like minimized tabs or colors.",
    ),
  ),
  const Chapter(
    id: 37,
    title: "Reading Network Symptoms",
    avatar: "🩺",
    role: "Narrator — playing network doctor",
    bodyIntro: "Good troubleshooting starts with matching a SYMPTOM to a LIKELY cause, then verifying with a tool (ping, traceroute, checking cables, etc.):\n\nMatch each symptom to its most likely explanation.",
    calloutHints: [
      "🩺 A real network engineer's instinct: \"high latency but zero loss\" usually means a slow/long path, not a broken link — while \"some latency AND heavy loss\" usually means real congestion or a failing link somewhere.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a symptom on the left, then its likely cause on the right.",
      pairs: [
        MatchPair("sym1", "High ping, but 0% packet loss", "Probably just a long physical/geographic path (e.g. satellite)"),
        MatchPair("sym2", "Normal ping, but 20% packet loss", "Likely congestion or a flaky link somewhere along the path"),
        MatchPair("sym3", "traceroute shows a huge delay jump at one specific hop", "That specific router/link is the likely trouble spot"),
      ],
      explainOk: "Exactly the kind of reasoning real network engineers use to localize problems before touching any configuration.",
    ),
  ),
  const Chapter(
    id: 38,
    title: "Design a Small Office Network",
    avatar: "🏢",
    role: "Professional Knowledge Check — network design",
    bodyIntro: "🏆 Level 10 — Professional Knowledge Check. A small office has 30 employees, needs Wi-Fi and wired desks, one shared printer, and a single Internet connection from an ISP. Design decisions matter now — not just vocabulary.\n\nOrder the buildout from the Internet connection down to individual desks.",
    calloutHints: [
      "💼 At this level, think like an engineer scoping real infrastructure: what device connects to the ISP? What distributes traffic locally? What separates guest Wi-Fi from internal systems for security?",
    ],
    puzzleType: PuzzleType.order,
    order: OrderPuzzle(
      instructions: "Drag these into the correct order, from the Internet connection down to a single employee's laptop.",
      items: [
        OrderItem("isp", "ISP hands off a single public IP at the building's edge router"),
        OrderItem("router", "Edge router/firewall does NAT and applies security rules"),
        OrderItem("switch", "A core switch distributes wired connections around the office"),
        OrderItem("ap", "Wireless access points give Wi-Fi coverage, on a separate guest VLAN if needed"),
        OrderItem("laptop", "An employee's laptop gets a private IP and connects to the switch or AP"),
      ],
      explainOk: "That's the real shape of small-business network design: edge router/firewall first, then distribution (switch/APs), then endpoints.",
      explainBad: "Work top-down from the Internet: ISP handoff → edge router (NAT+security) → switch/APs distribute it → individual devices last.",
    ),
  ),
  const Chapter(
    id: 39,
    title: "Diagnose the Outage",
    avatar: "🚨",
    role: "Professional Knowledge Check — incident response",
    bodyIntro: "Users report \"the Internet is down!\" You SSH into the edge router (over a separate management link) and find: the router can ping its own gateway and every internal device, but cannot ping anything outside the office network. DNS lookups also fail.\n\nGiven these symptoms, what's the most likely failure point?",
    calloutHints: [
      "🔬 Real incident response: isolate WHERE in the stack the failure is — is it local (LAN), the edge connection (WAN), or a specific service (DNS)?",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Internal LAN traffic works fine, but nothing external works (including DNS). What's the MOST likely cause?",
      options: [
        "A single employee's laptop has a bad Wi-Fi card",
        "The WAN link to the ISP is down, so nothing can leave the local network at all",
        "The office printer is out of paper",
        "One switch port on the 3rd floor is misconfigured",
      ],
      answerIndex: 1,
      explainOk: "Right! LAN works but nothing external does — including DNS, which is itself an external lookup — points straight at the WAN/ISP link being down, not a local device issue.",
      explainBad: "If EVERY external service fails at once (including DNS) while internal LAN traffic is fine, the failure is upstream of the LAN — the WAN/ISP connection itself, not one device or port.",
    ),
  ),
  const Chapter(
    id: 40,
    title: "Scaling for Reliability",
    avatar: "📈",
    role: "Professional Knowledge Check — production reliability",
    bodyIntro: "A growing web service now serves millions of requests. A single server is no longer enough, and a single point of failure is risky. Two production techniques help:\n\nLoad balancing — spreads incoming requests across many servers, so no single machine gets overwhelmed and traffic keeps flowing even if one server fails\n\nRedundancy — having backup routers/links/servers ready to take over instantly if the primary one fails, avoiding a single point of failure\n\nMatch the reliability technique to what it actually solves.",
    calloutHints: [
      "🏗️ Real production networks combine BOTH: redundant links so no single cable/router failure takes the whole system down, plus load balancers so no single server is a bottleneck.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a technique on the left, then the problem it solves on the right.",
      pairs: [
        MatchPair("lb", "Load balancing", "Prevents any single server from being overwhelmed by traffic"),
        MatchPair("redundancy", "Redundant links/hardware", "Avoids a single point of failure taking the whole system down"),
      ],
      explainOk: "Exactly the reasoning production network/systems engineers use daily — this is real infrastructure design thinking.",
    ),
  ),
  const Chapter(
    id: 41,
    title: "The Final Networking Gauntlet",
    avatar: "🏆",
    role: "Professional Knowledge Check — Shard Trial",
    bodyIntro: "⚔️ This is it — the final challenge of Network Nation. Everything from packets to TLS handshakes to production reliability comes together here.\n\nReason through the diagnosis to close the last circuit of Network Nation.",
    calloutHints: [
      "🌐 A real interview-style scenario: a user reports \"the site loads for me, but my coworker on the same Wi-Fi says it's really slow, and traceroute shows normal hops until hop 6, where latency spikes from 20ms to 400ms, with 15% packet loss — every hop AFTER that stays consistently bad too.\"",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Given the traceroute symptom (normal until hop 6, then latency/loss spikes and STAYS bad for every hop after), what's the most likely explanation?",
      options: [
        "The user's own laptop Wi-Fi card is failing",
        "Hop 6 itself (a router or link at that point) is congested or failing, and it drags down everything measured beyond it",
        "DNS is misconfigured on the user's machine",
        "The website's HTML has a bug",
      ],
      answerIndex: 1,
      explainOk: "🎉 Exactly right! When a problem appears at one hop and persists for every hop AFTER it, the trouble is AT that hop, not before or after — a classic real-world network diagnosis pattern. Network Nation's Shard is restored!",
      explainBad: "Key traceroute-reading skill: if degradation starts at one specific hop and continues for every hop after it, the problem lives at THAT hop — not the user's own device (which would show up at hop 1) and not something downstream-only like DNS or website code.",
    ),
  ),
  const Chapter(
    id: 42,
    title: "Load Balancers & Reverse Proxies",
    avatar: "⚖️",
    role: "Professional Knowledge Check — The Deployment Frontier",
    bodyIntro: "🏆 Level 11 — The Deployment Frontier. Welcome to production networking, where \"it works on my laptop\" meets millions of real requests. First stop: the load balancer.\n\nA Layer 4 (L4) load balancer works at the TCP/UDP level — it only looks at IP addresses and ports, forwarding whole connections without inspecting the actual HTTP traffic inside. It's fast and protocol-agnostic.\n\nA Layer 7 (L7) load balancer — often called a reverse proxy (like NGINX, Envoy, or a cloud ALB) — actually terminates and reads the HTTP request: headers, cookies, paths. That lets it route /api/* to one service and /static/* to another, do TLS termination, and rewrite headers.\n\n🧩 Match the load-balancing concept to what it actually does.",
    calloutHints: [
      "⚙️ Common algorithms: round robin (take turns), least connections (send to the least-busy server), and weighted (send more traffic to bigger boxes). All of them rely on health checks — a server that stops responding gets pulled out of rotation automatically.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a term on the left, then its correct description on the right.",
      pairs: [
        MatchPair("l4", "L4 load balancer", "Forwards TCP/UDP connections by IP/port, without reading HTTP content"),
        MatchPair("l7", "L7 reverse proxy", "Reads HTTP headers/paths to route requests to different backend services"),
        MatchPair("leastconn", "Least-connections algorithm", "Sends new traffic to whichever backend currently has the fewest active connections"),
        MatchPair("healthcheck", "Health check", "Automatically removes an unresponsive server from rotation"),
      ],
      explainOk: "Exactly right — L4 is fast and blind to content, L7 is content-aware and enables path-based routing, and health checks keep the pool honest in real time.",
    ),
  ),
  const Chapter(
    id: 43,
    title: "CDNs & Edge Caching",
    avatar: "🌍",
    role: "Professional Knowledge Check — The Deployment Frontier",
    bodyIntro: "A CDN (Content Delivery Network) caches your content at edge servers (POPs — points of presence) physically close to users, so a user in Tokyo doesn't have to round-trip to your origin server in Virginia for every image.\n\nCaching behavior is controlled by HTTP headers on the origin's response:\n\nCache-Control: public, max-age=86400, stale-while-revalidate=60\n\nETag: \"a1b2c3d4\"\n\nmax-age sets the TTL (how long the edge can serve it without asking the origin again). stale-while-revalidate lets the edge serve a slightly stale copy immediately while it refetches in the background — trading a little freshness for a much faster response. ETag lets the edge send a lightweight \"has this changed?\" check instead of re-downloading the whole asset.\n\nWhat's the real tradeoff behind stale-while-revalidate?",
    calloutHints: [
      "🚨 A common production incident: someone ships a bug, fixes it, but the CDN keeps serving the CACHED broken version for the full TTL — until someone remembers to trigger a cache purge/invalidation across every edge node, which itself takes time to propagate globally.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "What does 'stale-while-revalidate' actually trade away, and what does it gain?",
      options: [
        "It trades security for speed by disabling HTTPS at the edge",
        "It trades a small chance of serving slightly outdated content for consistently fast responses, refreshing in the background",
        "It permanently disables caching for that resource",
        "It forces every request to hit the origin server directly",
      ],
      answerIndex: 1,
      explainOk: "Right! The edge can answer instantly with a (maybe slightly stale) cached copy while quietly refetching a fresh one behind the scenes — great for perceived speed on content that doesn't need to be byte-perfect every millisecond.",
      explainBad: "stale-while-revalidate is about serving a possibly-stale cached response immediately for speed, then updating the cache asynchronously — it doesn't touch security, and it doesn't disable caching or force origin hits.",
    ),
  ),
  const Chapter(
    id: 44,
    title: "Service Discovery",
    avatar: "🧭",
    role: "Professional Knowledge Check — The Deployment Frontier",
    bodyIntro: "In a world of autoscaling and constantly-changing container IPs, hardcoding 10.0.4.17 into your config is a disaster waiting to happen. Service discovery is how services find each other's current, correct network location.\n\nServer-side discovery — the client just calls a stable name (e.g. through a load balancer or DNS record); something else behind that name figures out the real, current instance to route to. The client stays simple.\n\nClient-side discovery — the client queries a service registry (like Consul, etcd, or ZooKeeper) directly, gets back a live list of healthy instances, and picks one itself (often with its own load-balancing logic).\n\nSort each scenario into the discovery style it describes.",
    calloutHints: [
      "💓 Either way, instances must register themselves on startup and send regular heartbeats — miss enough heartbeats, and the registry marks that instance unhealthy and stops handing it out to new callers, even before it's fully dead.",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "Drag each scenario into the discovery pattern it matches.",
      bucketALabel: "🖥️ Server-side discovery (client stays simple)",
      bucketBLabel: "📡 Client-side discovery (client queries a registry itself)",
      items: [
        Sort2Item("d1", "A microservice calls a fixed internal DNS name behind a load balancer that picks the real backend", true),
        Sort2Item("d2", "A service queries Consul directly for the live list of healthy 'orders-api' instances, then picks one", false),
        Sort2Item("d3", "The client never sees individual instance IPs — routing logic lives entirely upstream", true),
        Sort2Item("d4", "The client implements its own load-balancing over a registry-provided instance list, and skips unhealthy entries itself", false),
      ],
      explainOk: "Right — server-side discovery hides the instance list behind something else (LB/DNS); client-side discovery hands the client the raw registry data and makes IT responsible for picking a healthy instance.",
      explainBad: "Ask: does the CLIENT itself talk to the registry and choose an instance (client-side), or does it just call a stable name and let something else route it (server-side)?",
    ),
  ),
  const Chapter(
    id: 45,
    title: "Blue-Green & Canary Deploys",
    avatar: "🐤",
    role: "Professional Knowledge Check — The Deployment Frontier",
    bodyIntro: "Shipping new code to production without downtime — or without risking every user at once — is a networking problem as much as a deployment one.\n\nBlue-green deploy: run the OLD version (\"blue\") and NEW version (\"green\") simultaneously on separate infrastructure. Once green is verified healthy, flip the load balancer/router to send 100% of traffic to green instantly. If something's wrong, flip back to blue just as instantly.\n\nCanary deploy: send a small SLICE of real traffic (say 5%) to the new version while 95% still hits the old one. Watch error rate and latency on that slice; if healthy, gradually shift more traffic over (5% → 25% → 100%). If it looks bad, automatically roll the weight back to 0%.\n\nOrder the steps of a real canary rollout.",
    calloutHints: [
      "📊 Canary rollouts live and die by their SLO (service-level objective) metrics — an automated system watching error rate and p99 latency on the canary slice can trigger an automatic rollback far faster than a human staring at a dashboard.",
    ],
    puzzleType: PuzzleType.order,
    order: OrderPuzzle(
      instructions: "Drag these into the order a production canary deploy actually proceeds.",
      items: [
        OrderItem("deploy", "Deploy the new version alongside the current stable version"),
        OrderItem("small", "Route a small percentage (e.g. 5%) of real traffic to the new version"),
        OrderItem("watch", "Monitor error rate and latency on that slice against SLO thresholds"),
        OrderItem("decide", "If healthy, increase traffic weight gradually; if unhealthy, automatically roll back to 0%"),
        OrderItem("full", "Once fully healthy at 100%, retire the old version"),
      ],
      explainOk: "That's a real canary pipeline — deploy alongside, expose a small slice, watch SLOs, ramp or roll back based on data, then finish the cutover.",
      explainBad: "Canaries never jump straight to 100% — they start small, watch real metrics against SLOs, and only ramp up traffic (or bail out) based on what those metrics say.",
    ),
  ),
  const Chapter(
    id: 46,
    title: "Consistent Hashing for Scale",
    avatar: "💍",
    role: "Professional Knowledge Check — The Scaling Wastes",
    bodyIntro: "🏆 Level 12 — The Scaling Wastes. When you distribute cache keys or requests across many nodes, a naive approach like hash(key) % N falls apart the moment N changes: adding or removing ONE server reshuffles almost EVERY key's target node, causing a massive, unnecessary cache-invalidation storm.\n\nConsistent hashing fixes this by placing both nodes AND keys onto a conceptual ring (0 to 2³²-1). Each key is assigned to the next node found going clockwise around the ring. When a node is added or removed, only the keys between it and its neighbor need to move — everything else stays put.\n\nWhat's the key advantage of consistent hashing over plain modulo hashing when scaling?",
    calloutHints: [
      "🔧 Real systems (like DynamoDB, Cassandra, and many CDNs) also use virtual nodes — each physical server owns MANY points on the ring, not just one — so load stays evenly balanced even with a small number of physical machines.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "A cluster grows from 4 to 5 nodes. With plain modulo hashing (hash(key) % N), roughly how many keys get remapped, compared to consistent hashing?",
      options: [
        "Both approaches remap the same tiny fraction of keys — there's no real difference",
        "Modulo hashing remaps almost ALL keys; consistent hashing only remaps the keys that land near the new node on the ring",
        "Consistent hashing remaps more keys because the ring is more complex",
        "Neither approach ever needs to remap keys when nodes change",
      ],
      answerIndex: 1,
      explainOk: "Right! Modulo hashing's mapping is fragile to N changing — almost every key's target shifts. Consistent hashing only disturbs the small neighborhood of ring-space near the added/removed node, which is why it's the standard choice for scalable caches and distributed stores.",
      explainBad: "The whole point of consistent hashing is minimizing churn: modulo hashing (%N) reshuffles nearly everything when N changes, while consistent hashing only moves keys near the affected node on the ring.",
    ),
  ),
  const Chapter(
    id: 47,
    title: "Connection Pooling",
    avatar: "🏊",
    role: "Professional Knowledge Check — The Scaling Wastes",
    bodyIntro: "Opening a brand-new TCP connection — and for HTTPS, a full TLS handshake on top of that — is expensive: multiple round-trips before a single byte of real data moves. At scale, doing this per-request would crush both latency and the downstream database or service.\n\nA connection pool keeps a set of already-established, already-warmed connections ready to reuse. A request \"checks out\" a connection, uses it, and \"checks it back in\" for the next caller — instead of tearing it down and rebuilding it.\n\nSort each situation into cause or effect of pool exhaustion.",
    calloutHints: [
      "⚠️ Pools have a real production failure mode: pool exhaustion. If every connection is checked out and busy (e.g. a slow downstream database), new requests QUEUE waiting for one to free up — and if that wait exceeds a client's timeout, you get cascading failures that look like a total outage, even though each individual query would have succeeded eventually.",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "Drag each item into whether it's a CAUSE of pool exhaustion or a SYMPTOM you'd observe.",
      bucketALabel: "🔧 Root cause",
      bucketBLabel: "🚨 Observed symptom",
      items: [
        Sort2Item("e1", "A downstream database is running slow queries, holding connections much longer than usual", true),
        Sort2Item("e2", "The pool size was configured far too small for actual peak concurrent traffic", true),
        Sort2Item("e3", "Requests start timing out waiting to check out a pooled connection", false),
        Sort2Item("e4", "Error rates spike across many unrelated endpoints all at once, even though each query itself is simple", false),
      ],
      explainOk: "Right — slow downstream queries and undersized pools are the actual root causes; timeouts-while-waiting and widespread simultaneous errors are the symptoms that show up on your dashboards.",
      explainBad: "Causes are WHY connections stay checked out too long (slowness, misconfiguration). Symptoms are what you SEE happen as a result — timeouts and broad, correlated errors.",
    ),
  ),
  const Chapter(
    id: 48,
    title: "Anycast & Global-Scale Routing",
    avatar: "🛰️",
    role: "Professional Knowledge Check — The Scaling Wastes",
    bodyIntro: "Big DNS resolvers (like 8.8.8.8) and CDNs advertise the SAME IP address from MANY data centers around the world simultaneously — this is anycast. Normal (unicast) routing sends a packet to one specific, unique destination; anycast lets the Internet's normal BGP routing naturally deliver a packet to whichever announcing location is \"closest\" in routing terms.\n\nAnycast also gives free DDoS resilience: an attack aimed at that IP address gets naturally SPREAD across every location announcing it, rather than concentrating all the attack traffic onto one data center.\n\nWhat determines which anycast location actually receives a given user's packet?",
    calloutHints: [
      "🧭 \"Closest\" here means closest in BGP path cost (fewest/cheapest AS hops), NOT necessarily closest in physical geography — a user in Chile could, in unusual cases, get routed to a POP in Brazil rather than a slightly-closer one in Argentina, if the Brazil route is genuinely cheaper in the global routing table.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Multiple data centers announce the exact same IP address via anycast. Which location does a given user's packet actually reach?",
      options: [
        "Always the physically nearest data center by straight-line distance",
        "Whichever location the normal Internet routing (BGP) considers the lowest-cost path from that user, which is usually — but not always — the nearest one",
        "A random data center chosen fresh for every single packet",
        "Whichever data center has the most free disk space at that moment",
      ],
      answerIndex: 1,
      explainOk: "Right! Anycast relies on ordinary BGP path-cost routing to decide the destination — that usually correlates with proximity, but it's really about routing cost, not literal geography or picking randomly.",
      explainBad: "Anycast doesn't measure physical distance directly, and it isn't random per-packet — it's whatever the normal BGP routing decides is the lowest-cost path, which usually (not always) ends up being the nearest location.",
    ),
  ),
  const Chapter(
    id: 49,
    title: "Backpressure & Flow Control at Scale",
    avatar: "🚰",
    role: "Professional Knowledge Check — The Scaling Wastes",
    bodyIntro: "We already met TCP's own flow control (the receive window) back in Level 5 — the receiver tells the sender \"only send this much before waiting for me.\" At the scale of a distributed system, the SAME idea has to happen at the application level too, or a slow downstream service gets buried alive.\n\nBackpressure is the mechanism by which a slow consumer signals upstream producers to slow down — instead of silently piling up an ever-growing, unbounded queue that eventually runs out of memory and crashes.\n\nBounded queues — reject or block new work once a queue hits its cap, rather than growing forever\n\nLoad shedding — deliberately drop or reject some requests (often the least important ones) to protect the system's ability to serve the rest\n\nCircuit breakers — stop calling a consistently-failing downstream service entirely for a while, instead of hammering it (and your own thread pool) with doomed requests\n\nMatch each backpressure technique to what it actually prevents.",
    calloutHints: [
      "🌊 Without backpressure, one slow dependency can cause an unbounded queue to balloon in memory upstream of it — turning a small local slowdown into a full system-wide outage minutes later.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a technique on the left, then what it prevents on the right.",
      pairs: [
        MatchPair("bounded", "Bounded queue", "Unbounded memory growth from work piling up faster than it's processed"),
        MatchPair("shed", "Load shedding", "Every request degrading together by sacrificing some requests to protect the rest"),
        MatchPair("breaker", "Circuit breaker", "Wasting resources on requests to a downstream service that's already failing"),
      ],
      explainOk: "Exactly — bounded queues cap memory risk, load shedding protects overall system health at the cost of some requests, and circuit breakers stop throwing good resources at a broken dependency.",
    ),
  ),
  const Chapter(
    id: 50,
    title: "The TLS Handshake, Deeper",
    avatar: "🔐",
    role: "Professional Knowledge Check — The Fortress of Trust",
    bodyIntro: "🏆 Level 13 — The Fortress of Trust. We saw the shape of the TLS handshake back in Level 7. Now let's see WHY it's built the way it is, using modern TLS 1.3.\n\nClientHello — client proposes cipher suites and sends an ephemeral ECDHE (Elliptic Curve Diffie-Hellman Ephemeral) public key share\n\nServerHello + Certificate — server picks a cipher suite, replies with its own ephemeral key share, and its X.509 certificate — a chain of trust from the server's cert up through an intermediate CA to a trusted root CA baked into your browser/OS\n\nKey derivation — both sides independently compute the SAME shared secret from the ECDHE exchange, without ever transmitting it\n\nFinished — both sides confirm, and encrypted application data flows\n\nWhy does TLS 1.3 insist on ephemeral (ECDHE) key exchange instead of just encrypting the session key directly with the server's long-term RSA public key?",
    calloutHints: [
      "🔑 Using a FRESH ephemeral key pair for every single connection (not a long-term key) gives forward secrecy: even if an attacker later steals the server's long-term private key, they still can't decrypt past traffic they recorded — each session's key died with that session.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "What real-world security property does ephemeral (ECDHE) key exchange provide that plain RSA key transport does not?",
      options: [
        "It makes the handshake use fewer round trips than RSA",
        "Forward secrecy — a later leak of the server's long-term private key can't be used to decrypt PAST recorded sessions",
        "It removes the need for a certificate entirely",
        "It guarantees the connection can never be intercepted by any party",
      ],
      answerIndex: 1,
      explainOk: "Right! With RSA key transport, one leaked private key retroactively unlocks every past recorded session encrypted with it. Ephemeral keys are thrown away after each session, so a future key leak can't touch old traffic — that's forward secrecy.",
      explainBad: "The specific property here is forward secrecy: because each session's key is temporary and discarded, a stolen long-term key later can't decrypt sessions that already happened. Certificates are still required either way, and no crypto guarantees zero interception attempts.",
    ),
  ),
  const Chapter(
    id: 51,
    title: "DDoS Mitigation Strategies",
    avatar: "🛡️",
    role: "Professional Knowledge Check — The Fortress of Trust",
    bodyIntro: "Not all DDoS attacks are the same, and neither are their defenses. Two broad categories, at different layers:\n\nVolumetric / L3-L4 attacks — brute-force floods of traffic (like UDP floods or SYN floods) meant to saturate bandwidth or exhaust connection tables. Defended with massive scrubbing centers and anycast absorption that spread and filter the flood before it ever reaches your real infrastructure.\n\nApplication-layer / L7 attacks — smaller volume but smarter, like floods of legitimate-looking HTTP requests to an expensive endpoint (e.g. a search or login form) designed to exhaust CPU/database capacity rather than bandwidth. Defended with rate limiting, WAFs (web application firewalls) that fingerprint malicious request patterns, and CAPTCHAs.\n\nSort each defense into the attack category it's best suited for.",
    calloutHints: [
      "🧯 A volumetric attack you can't out-scale locally — you need upstream capacity (scrubbing/anycast) to absorb terabits of traffic. An L7 attack you usually CAN mitigate locally, because it's really about smart filtering, not raw bandwidth.",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "Drag each defense technique into the attack layer it's designed to fight.",
      bucketALabel: "🌊 Volumetric (L3/L4) defenses",
      bucketBLabel: "🎯 Application-layer (L7) defenses",
      items: [
        Sort2Item("f1", "Anycast-based scrubbing centers absorbing a multi-terabit UDP flood", true),
        Sort2Item("f2", "A WAF fingerprinting and blocking a flood of malicious-looking login requests", false),
        Sort2Item("f3", "Upstream ISP-level filtering of a SYN flood before it reaches your data center", true),
        Sort2Item("f4", "Rate-limiting a single expensive search endpoint per API key", false),
      ],
      explainOk: "Right — raw bandwidth floods need upstream absorption at massive scale; smart-but-lower-volume application attacks need local, pattern-aware filtering like WAFs and rate limits.",
      explainBad: "Ask: is this attack about sheer VOLUME overwhelming pipes/tables (needs upstream scrubbing/anycast), or about smart-looking requests overwhelming app logic (needs local rate limiting/WAF)?",
    ),
  ),
  const Chapter(
    id: 52,
    title: "Zero-Trust Networking",
    avatar: "🕵️",
    role: "Professional Knowledge Check — The Fortress of Trust",
    bodyIntro: "Traditional network security drew a hard line: strong defenses at the perimeter (firewall at the edge), and implicit trust for anything already INSIDE that perimeter — \"if you're on the corporate network, you're trusted.\" That model breaks down badly once a single compromised laptop, or a hacked internal service, is already inside.\n\nZero-trust networking flips the assumption: never trust, always verify — regardless of whether a request comes from \"inside\" or \"outside\" the network. Every request, from every source, is authenticated and authorized on its own merits, every time.\n\nWhat's the core assumption that zero-trust networking rejects?",
    calloutHints: [
      "🪪 In practice this means: strong per-service identity (often via short-lived certificates), mutual authentication between services (not just the client proving who it is — the server proves it too), and fine-grained authorization checked on every single call, not once at the network edge.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "Which traditional networking assumption does zero-trust architecture specifically reject?",
      options: [
        "The assumption that encryption is ever necessary",
        "The assumption that being 'inside' the network perimeter should grant implicit trust",
        "The assumption that firewalls should exist at all",
        "The assumption that IP addresses are ever useful for anything",
      ],
      answerIndex: 1,
      explainOk: "Right! Zero-trust doesn't get rid of firewalls or encryption — it rejects the idea that simply being inside the perimeter should earn you automatic trust. Every request gets verified regardless of its origin.",
      explainBad: "Zero-trust isn't anti-firewall or anti-encryption — its actual target is the old assumption that 'inside the perimeter = trusted.' It insists on verifying every request, everywhere, every time.",
    ),
  ),
  const Chapter(
    id: 53,
    title: "VPNs vs mTLS",
    avatar: "🔏",
    role: "Professional Knowledge Check — The Fortress of Trust",
    bodyIntro: "Both VPNs and mTLS create secure, encrypted communication — but they solve different problems, and modern zero-trust architectures increasingly favor mTLS for service-to-service traffic.\n\nVPN — creates an encrypted tunnel and typically extends network-level access: once connected, a device often behaves as if it's ON that private network, able to reach many internal resources by IP. It authenticates the USER/device once, at connection time.\n\nmTLS (mutual TLS) — BOTH sides present certificates and verify each other on every single connection (not just the client verifying the server, like normal HTTPS). It authenticates each individual SERVICE call, with no implicit broader network access granted.\n\nMatch the technology to its actual trust model.",
    calloutHints: [
      "🏗️ A zero-trust service mesh typically uses mTLS between every pair of services, because it enforces per-call, per-identity verification — exactly the \"never trust, always verify\" principle — rather than a VPN's coarser \"you're on the network now, go anywhere\" model.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a technology on the left, then its trust model on the right.",
      pairs: [
        MatchPair("vpn", "VPN", "Authenticates once at connect time, then grants broader network-level access"),
        MatchPair("mtls", "mTLS", "Both sides verify each other's certificate on every individual connection, with no implicit broader access"),
        MatchPair("https", "Standard HTTPS (one-way TLS)", "Only the CLIENT verifies the server's certificate — the server doesn't verify the client's identity"),
      ],
      explainOk: "Right — VPNs grant broad, session-based network access after one login; mTLS enforces per-call, mutual identity verification; plain HTTPS only proves the server's identity to the client, not the reverse.",
    ),
  ),
  const Chapter(
    id: 54,
    title: "Service Mesh & Sidecars",
    avatar: "🧩",
    role: "Professional Knowledge Check — The Architect's Labyrinth",
    bodyIntro: "🏆 Level 14 — The Architect's Labyrinth. In a microservices architecture with dozens or hundreds of services, every team re-implementing retries, timeouts, mTLS, and observability inside their own application code doesn't scale. A service mesh moves that networking logic OUT of the application.\n\nEach service gets a sidecar proxy (like Envoy) deployed alongside it — every bit of network traffic in and out of the service actually flows through its sidecar first, transparently. The sidecars are configured centrally by a control plane (like Istio's control plane), which pushes down routing rules, retry policies, and mTLS certificates to every sidecar in the mesh.\n\nWhat's the core architectural benefit of the sidecar pattern?",
    calloutHints: [
      "🔍 Because ALL traffic passes through sidecars, the mesh gets automatic mTLS between every service, automatic retries/timeouts/circuit breaking, and rich observability (latency, error rates per service pair) — without a single line of that logic living in any application's own code.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "What is the main architectural advantage of putting networking logic (retries, mTLS, observability) into a sidecar proxy instead of application code?",
      options: [
        "It makes the application code run measurably faster",
        "It decouples cross-cutting networking concerns from business logic, so every service gets consistent behavior without each team reimplementing it",
        "It eliminates the need for a network entirely",
        "It removes the need for load balancers",
      ],
      answerIndex: 1,
      explainOk: "Right! The sidecar pattern's whole point is separation of concerns at scale — networking behavior (retries, mTLS, metrics) becomes uniform infrastructure, not something every team hand-rolls differently in their own service.",
      explainBad: "Sidecars aren't primarily about raw speed — they're about consistently handling cross-cutting networking concerns (retries, security, observability) OUTSIDE application code, so every service in the mesh behaves the same way for free.",
    ),
  ),
  const Chapter(
    id: 55,
    title: "CAP Theorem Tradeoffs",
    avatar: "⚡",
    role: "Professional Knowledge Check — The Architect's Labyrinth",
    bodyIntro: "The CAP theorem says a distributed system, when a network partition happens (some nodes can't reach others), must choose between:\n\nConsistency (C) — every node sees the same, most up-to-date data, even during the partition (which may mean refusing some requests until it's resolved)\n\nAvailability (A) — every request still gets SOME response, even if it might be stale or based on only part of the data\n\n(Partition tolerance isn't really optional for real distributed systems — networks WILL partition eventually — so in practice this is a C vs A choice DURING a partition.)\n\nSort each real system by the tradeoff it most likely makes during a network partition.",
    calloutHints: [
      "🏦 A banking ledger service usually chooses CP: better to reject a transaction during a partition than risk an inconsistent balance. A social media \"like\" counter usually chooses AP: better to keep showing SOMETHING (even if briefly stale) than go fully unavailable over a minor network hiccup.",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "Drag each system into the choice it most likely makes when a network partition occurs.",
      bucketALabel: "🔒 CP — favors Consistency, may sacrifice Availability",
      bucketBLabel: "🌍 AP — favors Availability, may serve stale data",
      items: [
        Sort2Item("g1", "A bank's core ledger, which must never show two different balances for the same account", true),
        Sort2Item("g2", "A shopping cart service that would rather show a slightly-stale cart than fail the whole page", false),
        Sort2Item("g3", "A distributed lock service coordinating who currently owns a critical resource", true),
        Sort2Item("g4", "A DNS-style read-heavy cache that keeps serving the last-known-good answer during a partition", false),
      ],
      explainOk: "Exactly the reasoning real architects apply — when correctness failures are catastrophic (money, locks), choose CP; when staying up matters more than perfect freshness, choose AP.",
      explainBad: "Ask: if this system returns a WRONG answer during a partition, is that catastrophic (choose CP, e.g. money/locks) or tolerable (choose AP, e.g. carts/caches that can self-correct later)?",
    ),
  ),
  const Chapter(
    id: 56,
    title: "API Gateway Design",
    avatar: "🚪",
    role: "Professional Knowledge Check — The Architect's Labyrinth",
    bodyIntro: "When a microservices architecture has dozens of backend services, you don't want every mobile app or browser client to know about (and call) each one directly. An API gateway sits at the edge as a single entry point, handling cross-cutting concerns so individual services don't each reinvent them:\n\nAuthentication/authorization — verify who's calling once, at the edge\n\nRate limiting — protect backend services from being overwhelmed by any single client\n\nRequest routing/aggregation — one client call might fan out to several backend services and get combined into one response (avoiding many mobile round-trips over a slow connection)\n\nProtocol translation — expose a clean REST/JSON API externally while backend services talk gRPC internally\n\nMatch the API gateway responsibility to its purpose.",
    calloutHints: [
      "⚠️ The tradeoff: the gateway becomes a critical single point that EVERY request flows through — it must be built for very high availability and low added latency, or it becomes the whole system's bottleneck instead of solving one.",
    ],
    puzzleType: PuzzleType.match,
    match: MatchPuzzle(
      instructions: "Click a responsibility on the left, then its purpose on the right.",
      pairs: [
        MatchPair("authn", "Authentication at the edge", "Verifies caller identity once, before requests reach any backend service"),
        MatchPair("rl", "Rate limiting", "Protects backend services from being overwhelmed by any single client"),
        MatchPair("agg", "Request aggregation", "Combines multiple backend calls into one response for the client, saving round-trips"),
        MatchPair("proto", "Protocol translation", "Exposes a clean external API while backends use a different internal protocol"),
      ],
      explainOk: "Right — a well-designed gateway centralizes identity checking, protects backends from overload, reduces client round-trips, and hides internal protocol choices from the outside world.",
    ),
  ),
  const Chapter(
    id: 57,
    title: "gRPC vs REST vs Message Queues",
    avatar: "🔀",
    role: "Professional Knowledge Check — The Architect's Labyrinth",
    bodyIntro: "Choosing HOW two services talk is a real architecture decision, not just a style preference:\n\nREST (over HTTP/JSON) — simple, human-readable, universally supported by browsers and every language. Great for public-facing APIs, but JSON parsing and HTTP/1.1-style overhead add latency for high-throughput internal calls.\n\ngRPC (over HTTP/2 + Protobuf) — binary, strongly-typed, supports streaming in both directions, and is significantly faster/lighter than REST/JSON. Ideal for low-latency internal service-to-service calls, but harder for browsers to call directly and less human-debuggable on the wire.\n\nMessage queues (Kafka, RabbitMQ, SQS) — fully asynchronous and decoupled: the sender doesn't wait for a response at all, and the receiver processes work whenever it's ready. Ideal when the caller doesn't need an immediate answer, or when you need to buffer against a downstream service being temporarily slow/down.\n\nSort each scenario into the communication style that fits it best.",
    calloutHints: [
      "🏗️ A real system often uses ALL three: REST for the public API, gRPC between internal microservices that need low-latency synchronous calls, and a message queue for background work like sending emails or processing uploaded videos.",
    ],
    puzzleType: PuzzleType.sort2,
    sort2: Sort2Puzzle(
      instructions: "Drag each scenario into the best-fit communication style. (Group any queue-appropriate scenario with the async option.)",
      bucketALabel: "⚡ Synchronous, low-latency (gRPC)",
      bucketBLabel: "📨 Asynchronous, decoupled (message queue)",
      items: [
        Sort2Item("s1", "An internal service needs an immediate response from another internal service, thousands of times per second", true),
        Sort2Item("s2", "A user uploads a video that needs to be transcoded — the upload should complete without waiting for transcoding to finish", false),
        Sort2Item("s3", "Two internal services stream continuous real-time position updates to each other with minimal overhead", true),
        Sort2Item("s4", "An order-placed event needs to notify billing, shipping, and analytics — none of which the caller should have to wait on", false),
      ],
      explainOk: "Right — anything needing a fast, immediate, synchronous answer (or bidirectional streaming) fits gRPC; anything that can happen later, independently, and shouldn't block the caller fits a message queue.",
      explainBad: "Ask: does the caller need an answer RIGHT NOW to keep working (→ gRPC-style sync call), or can the work happen later, independently, without blocking the caller (→ message queue)?",
    ),
  ),
  const Chapter(
    id: 58,
    title: "Cascading Timeouts — A Production Autopsy",
    avatar: "🩻",
    role: "Principal Engineer Gauntlet — The Machine's Reckoning",
    bodyIntro: "🏆 Level 15 — The Machine's Reckoning. This is the hardest tier in Network Nation, synthesizing everything from Levels 11-14 into real incident analysis.\n\nIncident: Service A calls Service B calls Service C. Service C starts responding slowly (but not failing outright) due to an unrelated database issue. Service B has no timeout configured on its calls to C, so its request threads pile up waiting on C's slow responses. Soon B's entire thread pool is exhausted waiting on C — so B can no longer respond to ANYONE, including Service A. Now A's calls to B start timing out too, and A's own thread pool starts filling up waiting on B. Within minutes, the ENTIRE system is down — even though only Service C's database was ever actually struggling.\n\nWhat is the SINGLE most important missing safeguard that would have prevented this cascade from propagating all the way back to Service A?",
    calloutHints: [
      "🧠 This is a cascading failure: a small, localized slowdown amplifies into a system-wide outage because nothing in the chain had a timeout, a circuit breaker, or a bulkhead (isolating one dependency's resource usage from another's) to stop the bleeding early.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "In this cascading-timeout incident, what is the most fundamental missing safeguard?",
      options: [
        "Service C's database needed a bigger server",
        "Service B needed a request timeout (and ideally a circuit breaker) on its calls to C, so its own thread pool couldn't be exhausted waiting indefinitely on a slow dependency",
        "Service A should have used UDP instead of TCP",
        "All three services should have been merged into one monolith",
      ],
      answerIndex: 1,
      explainOk: "🎉 Exactly right! The root enabler of the cascade wasn't C being slow (that will always happen eventually) — it was B having no timeout/circuit breaker, letting one slow dependency consume ALL of B's capacity and take down everyone who depends on B. That's the classic cascading-failure pattern seen in real production incidents.",
      explainBad: "The database being slow is the TRIGGER, but the actual root cause of the CASCADE is that Service B had no timeout/circuit breaker protecting its own thread pool from a slow dependency — that's what let the failure spread instead of staying contained.",
    ),
  ),
  const Chapter(
    id: 59,
    title: "A Routing Failure at Internet Scale",
    avatar: "🗺️",
    role: "Principal Engineer Gauntlet — The Machine's Reckoning",
    bodyIntro: "Real Internet routing between huge networks uses BGP (Border Gateway Protocol) — routers announce \"I can reach this block of IP addresses\" to their neighbors, who pass those announcements along, gradually building a global map of who can reach what.\n\nIncident: A network operator misconfigures their router and accidentally announces that THEY are the best path to a huge block of IP addresses they don't actually control or have good connectivity to — a route leak (similar in spirit to a route hijack). Because BGP largely trusts announcements from peers, other networks start sending traffic for those addresses toward the misconfigured network — which then black-holes it (drops it, since it never really had a usable path there), or routes it through a slow, congested link never designed for that volume.\n\nWhat made this incident possible at its core — and what's the analogous internal-system risk it should remind you of?",
    calloutHints: [
      "🧭 This mirrors internal networking failures you've already seen: a service registry advertising WRONG weights/endpoints for a service is functionally the same failure pattern at a smaller scale — traffic gets misdirected toward a destination that can't actually handle it, based on trusting an announcement instead of verifying real capacity.",
    ],
    puzzleType: PuzzleType.mcq,
    mcq: McqPuzzle(
      question: "What is the fundamental vulnerability this BGP route leak incident exposes, and what internal-system pattern is it analogous to?",
      options: [
        "It's purely a hardware failure with no analogy to software systems",
        "Routing systems (BGP, and analogously service discovery/registries) largely trust announcements from participants without independently verifying real capacity or authority — a bad announcement can misdirect traffic system-wide",
        "It only happens because IPv4 addresses are running out",
        "It's caused by using UDP instead of TCP for routing announcements",
      ],
      answerIndex: 1,
      explainOk: "Right! Both BGP and internal service discovery/registries operate on a trust model — they largely believe what participants announce about their own reachability/capacity. A bad (accidental or malicious) announcement can misdirect huge amounts of traffic before anyone notices and corrects it — the exact same failure shape, just at different scales.",
      explainBad: "This isn't about hardware, address exhaustion, or transport protocol choice — it's about a TRUST model: routing/discovery systems believe announcements about reachability/capacity without independent verification, so one bad announcement can misdirect traffic broadly, whether it's global BGP or an internal service registry.",
    ),
  ),
  const Chapter(
    id: 60,
    title: "Design a Multi-Region Failover",
    avatar: "🌐",
    role: "Principal Engineer Gauntlet — The Machine's Reckoning",
    bodyIntro: "Your service now runs active infrastructure in two regions (US-East and EU-West) for both scale AND disaster recovery. You must design what happens when US-East suffers a full regional outage — synthesizing load balancing, service discovery, anycast/DNS routing, and data consistency all at once.\n\nOrder the real steps of executing this regional failover.",
    calloutHints: [
      "💾 The hardest part usually isn't the network routing — it's DATA. If US-East's database was the primary and EU-West only had a replica, you must decide: does EU-West promote itself to primary immediately (favoring Availability, CAP-theorem style, risking losing the last few unreplicated writes), or does it wait for confirmation that US-East is truly gone and data is safe to reconcile (favoring Consistency, at the cost of downtime)?",
    ],
    puzzleType: PuzzleType.order,
    order: OrderPuzzle(
      instructions: "Drag these into the correct order for a real multi-region failover.",
      items: [
        OrderItem("detect", "Automated health checks confirm US-East is unreachable across multiple independent signals (not just one flaky check)"),
        OrderItem("decide", "Decide the data-consistency tradeoff: promote EU-West's replica to primary now (AP) vs wait for safe reconciliation (CP)"),
        OrderItem("reroute", "Update global DNS/anycast routing so new traffic is directed to EU-West instead of the dead region"),
        OrderItem("drain", "Allow in-flight requests still bound for US-East to fail fast/retry, rather than hang indefinitely"),
        OrderItem("verify", "Verify EU-West is handling full load correctly before declaring the failover complete"),
      ],
      explainOk: "That's a real regional failover: confirm the failure is real, make the hard consistency-vs-availability call up front, redirect traffic, stop hanging on the dead region, then verify the new region is actually holding up under full load.",
      explainBad: "You must first CONFIRM the region is really down (avoid a false failover), then make the CAP tradeoff decision BEFORE rerouting (it shapes how you handle in-flight data), then redirect traffic, drain stuck requests, and finally verify the new region can truly handle full load.",
    ),
  ),
  const Chapter(
    id: 61,
    title: "The Final Circuit: Safe Rollback",
    avatar: "🏆",
    role: "Principal Engineer Gauntlet — The Machine's Reckoning",
    bodyIntro: "⚔️ The final challenge of Network Nation's professional tier. A canary deployment (Level 11) is rolling out. Its automated guardrail must satisfy an AND condition before it's allowed to declare \"safe to keep rolling out\": the SLO error-rate budget must NOT be breached, AND the circuit breaker protecting the downstream dependency must be in its healthy (closed) state.\n\nLet A = \"error budget still healthy\" (1 if true) and B = \"circuit breaker closed/healthy\" (1 if true). Only when BOTH are true does the rollout continue automatically — if EITHER one goes unhealthy, the system must instead trigger an automatic rollback, exactly the kind of production safeguard that would have prevented the cascading-timeout incident from Chapter LVIII.\n\nClose the final circuit: flip both switches to confirm the rollout is genuinely safe to continue.",
    calloutHints: [
      "🌐 You've now walked the full professional path: production deployment mechanics, scaling patterns, security and trust models, architectural tradeoffs, and real incident diagnosis. That's the genuine shape of senior network/infra engineering — not just vocabulary, but judgment under real production pressure.",
    ],
    puzzleType: PuzzleType.circuit,
    circuit: CircuitPuzzle(
      instructions: "A = error budget healthy, B = circuit breaker closed/healthy. Flip both switches to 1 so the 'safe to continue rollout' LED lights up.",
      gate: CircuitGate.and,
      explainOk: "🎉 Both conditions true means it's genuinely safe to keep rolling out — exactly the AND-gate discipline that stops a cascading failure before it starts. Network Nation's final Shard is restored — you've completed the Professional Tier!",
      explainBad: "This AND gate only lights up when BOTH the error budget AND the circuit breaker are healthy — a real rollout guardrail requires every safety condition to hold, not just one of them.",
    ),
  ),
];
